// -  Create variable named `nameToGreet` and assign the value `Green Fox` to it
let nameToGreet = "Green Fox";
// -  Create a function called `greet` that greets it's input parameter
function greet(input: string) {
    console.log("Greetings, dear " + input + "!") // -  Greeting is printing e.g. `Greetings, dear Green Fox`
}
greet(nameToGreet); // -  Greet `nameToGreet`